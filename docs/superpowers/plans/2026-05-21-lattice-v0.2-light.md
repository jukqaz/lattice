# Lattice v0.2 Light Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make Lattice safe enough for real personal dotfiles by adding restore conflict detection, snapshots, minimal hooks, secret guardrails, and stronger harness coverage.

**Architecture:** Keep the product lightweight. Extend the existing TOML service model and copy-based operations instead of introducing a database, plugin system, or git sync. Put safety logic in focused library modules and keep CLI code as command orchestration.

**Tech Stack:** Rust 2024, `clap`, `serde`, `toml`, standard library filesystem/process APIs, existing `xtask` harness.

---

## File Structure

- `src/config.rs`: add hook config models.
- `src/hooks.rs`: run or describe command hooks by phase.
- `src/secrets.rs`: lightweight secret-looking content scan.
- `src/ops.rs`: restore conflict detection, snapshots, force behavior, and backup secret scan wiring.
- `src/main.rs`: add `--force`, `--yes`, `--allow-secret-looking-files`, and `validate`.
- `src/lib.rs`: export new modules.
- `tests/cli_smoke.rs`: cover conflicts, snapshots, hooks, custom services, and secret scan CLI behavior.
- `xtask/src/main.rs`: add real `~/.codex` read-only dry-run coverage.
- `TODO.md`: mark v0.2 items as implemented only after verification.

## Task 1: Restore Conflicts And Snapshots

- [ ] Write failing tests showing `restore codex` refuses conflicting destination files by default.
- [ ] Write failing tests showing `restore --force codex` overwrites conflicts and creates a snapshot under XDG state.
- [ ] Implement conflict detection and snapshot copy in `src/ops.rs`.
- [ ] Wire `--force` and state snapshot root through `src/main.rs`.
- [ ] Run `cargo run -p xtask -- verify`.

## Task 2: Minimal Lifecycle Hooks

- [ ] Write failing tests for `hooks.after_restore` dry-run output without execution.
- [ ] Write failing tests for hook execution order and `confirm = true` requiring `--yes`.
- [ ] Add hook config models in `src/config.rs`.
- [ ] Implement command hook execution in `src/hooks.rs` with timeout metadata accepted but not process-killed yet.
- [ ] Wire hooks around backup/restore in `src/main.rs`.
- [ ] Run `cargo run -p xtask -- verify`.

## Task 3: Secret Guardrail And Custom Service Fixture

- [ ] Write failing tests proving backup blocks obvious token-looking content.
- [ ] Write failing tests proving `--allow-secret-looking-files` bypasses that block.
- [ ] Add a custom non-Codex service fixture to the CLI smoke test.
- [ ] Implement `src/secrets.rs` and wire it before backup writes.
- [ ] Run `cargo run -p xtask -- verify`.

## Task 4: Validate And Real Codex Dry-Run Harness

- [ ] Write failing CLI test for `lattice validate`.
- [ ] Implement `validate` by parsing global config and all service TOML files.
- [ ] Extend `xtask verify` to run real `~/.codex` status and backup dry-run against an isolated XDG config when `~/.codex` exists.
- [ ] Run `cargo run -p xtask -- verify`.

## Task 5: Release Closure

- [ ] Update README and TODO for v0.2.
- [ ] Run `cargo run -p xtask -- verify`.
- [ ] Run `cargo package`.
- [ ] Commit, tag `v0.2.0`, push, and create a GitHub release if verification passes.

